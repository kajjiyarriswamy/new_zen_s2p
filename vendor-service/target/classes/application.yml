server:
  port: 8086

spring:
  kafka:
    bootstrap-servers: localhost:9092

  datasource:
    url: jdbc:mysql://localhost:3306/ams
    username: root
    password: Abhishek@1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
